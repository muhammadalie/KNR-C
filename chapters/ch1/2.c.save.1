#include<stdio.h>
//int lower(int c);
//int binsearch(int x, int v[], int n);
main()
{
	int v[10]={1,3,8,9,11,16,27,35,48,'\0'};
	
	 int low, high, mid,x=9,n=9;
        low = 0;
        high = n - 1;
        while (low <= high) {
               // mid = (low+high)/2;
                if (x < v[n])
                       // high = mid + 1;
              //  else if (x > v[mid])
                        low =  + 1;
                else
                        return mid;

        }
        //return -1;
	
	
	//printf("%d",binsearch(4,v,9));




}


///*
int binsearch(int x, int v[], int n)
{
	int low, high, mid;
	low = 0;
	high = n - 1;
	while (low <= high) {
		mid = (low+high)/2;
		if (x < v[mid])
			high = mid + 1;
		else if (x > v[mid])
			low = mid + 1;
		else
			return mid;
	}
	return -1;

}
*/
